{-
   Quant Overledger API

   Quant’s Overledger API allows developers to create applications for multiple DLT’s using a single standard set of operations and data structures.In order to maintain the security of private keys, most operations have two steps – prepare and execute. The prepare step is the point at which all arguments are specified and standardised payloads are sent. Overledger converts this standard payload into a DLT-specific transaction object. In the execute step, the SDK signs the transaction object that Overledger created and submits it to Overledger to perform the operation

   OpenAPI Version: 3.0.1
   Quant Overledger API API version: 2.0
   Generated by OpenAPI Generator (https://openapi-generator.tech)
-}

{-|
Module : QuantOverledger.API.RetrieveOwnerInformationForAQRC721Token
-}

{-# LANGUAGE FlexibleContexts #-}
{-# LANGUAGE FlexibleInstances #-}
{-# LANGUAGE MonoLocalBinds #-}
{-# LANGUAGE MultiParamTypeClasses #-}
{-# LANGUAGE OverloadedStrings #-}
{-# OPTIONS_GHC -fno-warn-name-shadowing -fno-warn-unused-binds -fno-warn-unused-imports #-}

module QuantOverledger.API.RetrieveOwnerInformationForAQRC721Token where

import QuantOverledger.Core
import QuantOverledger.MimeTypes
import QuantOverledger.Model as M

import qualified Data.Aeson as A
import qualified Data.ByteString as B
import qualified Data.ByteString.Lazy as BL
import qualified Data.Data as P (Typeable, TypeRep, typeOf, typeRep)
import qualified Data.Foldable as P
import qualified Data.Map as Map
import qualified Data.Maybe as P
import qualified Data.Proxy as P (Proxy(..))
import qualified Data.Set as Set
import qualified Data.String as P
import qualified Data.Text as T
import qualified Data.Text.Encoding as T
import qualified Data.Text.Lazy as TL
import qualified Data.Text.Lazy.Encoding as TL
import qualified Data.Time as TI
import qualified Network.HTTP.Client.MultipartFormData as NH
import qualified Network.HTTP.Media as ME
import qualified Network.HTTP.Types as NH
import qualified Web.FormUrlEncoded as WH
import qualified Web.HttpApiData as WH

import Data.Text (Text)
import GHC.Base ((<|>))

import Prelude ((==),(/=),($), (.),(<$>),(<*>),(>>=),Maybe(..),Bool(..),Char,Double,FilePath,Float,Int,Integer,String,fmap,undefined,mempty,maybe,pure,Monad,Applicative,Functor)
import qualified Prelude as P

-- * Operations


-- ** RetrieveOwnerInformationForAQRC721Token

-- *** readQRC721SmartContractTokenOwner

-- | @POST \/v2\/tokenise\/tokens\/qrc721\/token-owner@
-- 
-- Retrieve which account currently owns a specific QRC721 token
-- 
-- Submits a query to the DLN and returns the address that currently has owns a particular QRC721 token.
-- 
-- AuthMethod: 'AuthOAuthOAuth2SecurityScheme'
-- 
readQRC721SmartContractTokenOwner
  :: (Consumes ReadQRC721SmartContractTokenOwner MimeJSON, MimeRender MimeJSON ReadTokenOwnerRequestSchema)
  => ReadTokenOwnerRequestSchema -- ^ "readTokenOwnerRequestSchema"
  -> Authorization -- ^ "authorization"
  -> QuantOverledgerRequest ReadQRC721SmartContractTokenOwner MimeJSON TokenReadQRC721Response MimeJSON
readQRC721SmartContractTokenOwner readTokenOwnerRequestSchema (Authorization authorization) =
  _mkRequest "POST" ["/v2/tokenise/tokens/qrc721/token-owner"]
    `_hasAuthType` (P.Proxy :: P.Proxy AuthOAuthOAuth2SecurityScheme)
    `setBodyParam` readTokenOwnerRequestSchema
    `addHeader` toHeader ("Authorization", authorization)

data ReadQRC721SmartContractTokenOwner 
instance HasBodyParam ReadQRC721SmartContractTokenOwner ReadTokenOwnerRequestSchema 

-- | @application/json@
instance Consumes ReadQRC721SmartContractTokenOwner MimeJSON

-- | @application/json@
instance Produces ReadQRC721SmartContractTokenOwner MimeJSON

